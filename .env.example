# Supabase Configuration (DEPRECATED - migration vers Turso/SQLite)
# NEXT_PUBLIC_SUPABASE_URL=your_supabase_project_url
# NEXT_PUBLIC_SUPABASE_ANON_KEY=your_supabase_anon_key
# SUPABASE_SERVICE_ROLE_KEY=your_supabase_service_role_key

# Database Configuration
# Pour développement local (SQLite) - Aucune configuration nécessaire
# La base sera créée automatiquement dans ./data/ciprel.db

# Pour déploiement sur Vercel avec Turso (décommentez ces lignes)
# TURSO_DATABASE_URL=libsql://your-database-name.turso.io
# TURSO_AUTH_TOKEN=your_auth_token_here

# Instructions pour créer une base Turso :
# 1. Installer Turso CLI : brew install tursodatabase/tap/turso
# 2. S'authentifier : turso auth signup
# 3. Créer une DB : turso db create ciprel
# 4. Obtenir l'URL : turso db show ciprel --url
# 5. Générer un token : turso db tokens create ciprel
# 6. Migrer le schéma : turso db shell ciprel < turso-schema.sql
# 7. Copier l'URL et le token dans .env.local

# Application Configuration
NEXT_PUBLIC_APP_URL=http://localhost:3000
NEXT_PUBLIC_SITE_NAME=CIPREL Compétences
NEXT_PUBLIC_COMPANY_NAME=CIPREL
NEXT_PUBLIC_COMPANY_EMAIL=contact@ciprel.ci

# GDPR Configuration
NEXT_PUBLIC_GDPR_ENABLED=true
