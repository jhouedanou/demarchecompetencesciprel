version: '3.8'

services:
  spfx-minimal:
    build:
      context: .
      dockerfile: Dockerfile.minimal
    container_name: spfx-minimal-test
    ports:
      - "4321:4321"
    environment:
      - NODE_ENV=development
      - HOST=0.0.0.0
    command: npm run serve

volumes:
  node_modules: